{
  "name": "@remote/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "test": "vitest run --root ../.. apps/server/tests",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json --noEmit"
  },
  "dependencies": {
    "@fastify/websocket": "^11.0.1",
    "@remote/protocol": "workspace:*",
    "fastify": "^5.2.1",
    "nanoid": "^5.0.9",
    "pg": "^8.20.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0",
    "@types/ws": "^8.5.13",
    "pg-mem": "^3.0.14"
  }
}
