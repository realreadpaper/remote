{
  "name": "@remote/mobile",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "expo start",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --root ../.. apps/mobile/tests",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json --noEmit"
  },
  "dependencies": {
    "@remote/protocol": "workspace:*",
    "expo": "^52.0.23",
    "react": "^18.3.1",
    "react-native": "^0.76.5"
  },
  "devDependencies": {
    "@types/react": "^18.3.18"
  }
}
